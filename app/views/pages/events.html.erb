<div id="wrapper">
 <%= render 'navigation'%>
		<div class="boardmember_title" style="background-color:#FCC956;">
				<p>Events and News</p>
		</div>

	<div class="news_events_border">
<% @news_top.each do |top| %>
				<div class="news_events_content">
					<h1> <%= top.news_title%></h1>

					<div class="news_events_image">
<%= image_tag "#{top.news_image}",:class=>"news_events_name"%>
					</div>		<!--end of board_image---->

					<div class="news_events_text">
						<p><%= top.news_desc%></p>

          </div>

</div>
<%end%>
</div>

<div class="line_image_border1">
</div>
</div>
<%= render 'image_slider'%>
<%= render 'lowercontent'%>
<%= render 'footer'%>
